# Server
HOST=0.0.0.0
PORT=8080

# WSL (Windows only - ignored on Linux/macOS)
WSL_DISTRO=Ubuntu

# Security
RATE_LIMIT_MAX=60
RATE_LIMIT_WINDOW=60

# Authentication token (auto-generated if not set)
# AUTH_TOKEN=your-secret-token-here

# CORS allowed origins (comma-separated, defaults to http://localhost:PORT)
# CORS_ORIGIN=http://localhost:8080,https://your-tailscale-host.ts.net

# Paths - auto-detected per platform if not set
# Windows default: /mnt/c/,/mnt/d/,/home/
# Linux default:   /home/,/tmp/,/opt/,/var/
# macOS default:   /Users/,/tmp/,/opt/,/var/
# ALLOWED_PATHS=/home/,/tmp/
# BLOCKED_PATHS=/etc,/root,/sys,/proc,/dev

# Default working directory for new sessions (auto-detected if not set)
# DEFAULT_PATH=/home/

# E2E encryption (AES-256-GCM) — encrypts all WebSocket terminal I/O
# Key is shared via URL fragment (#key) and never sent to the server in HTTP requests
# E2E_ENABLED=true
